@model List<Category>

@foreach (Category category in Model)
{
    <div>
        <a asp-controller="Category" asp-action="OneCategory" asp-route-CategoryId="@category.CategoryId"> @category.Name</a>
    </div>
}