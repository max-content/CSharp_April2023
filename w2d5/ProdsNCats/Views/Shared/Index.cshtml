@{
    ViewData["Title"] = "Home Page";
}

@model IndexViewModel

<div class="container d-flex">
    <div class="col-6"> 
        <h1> New Product </h1>
        @await Html.PartialAsync("_NewProduct", Model.Product)
    </div>
    <div class="col-6">
        <h1> All Products </h1>
        @await Html.PartialAsync("_ShowProducts", Model.AllProducts)
    </div>
</div>